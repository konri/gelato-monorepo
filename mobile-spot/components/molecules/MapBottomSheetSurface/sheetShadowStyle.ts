import type { ViewStyle } from 'react-native';

export const MAP_BOTTOM_SHEET_CARD_SHADOW: ViewStyle = {
  shadowColor: '#000',
  shadowOffset: { width: 0, height: -4 },
  shadowOpacity: 0.12,
  shadowRadius: 8,
  elevation: 16,
};

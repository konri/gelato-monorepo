export type MerchantInput = {
  name: string;
  description?: string;
  logoUrl?: string;
  coverUrl?: string;
  iconUrl?: string;
  categoryId: string;
};

export type MerchantBasic = {
  id: string;
  name: string;
  description?: string;
  logoUrl?: string;
  coverUrl?: string;
  iconUrl?: string;
  categoryId: string;
};

